{
	"servers": {
		"Currency Converter": {
			"type": "stdio",
			// If the default "python" command doesn't work, get the venv Python
			// First, ensure the venv is activated (mcp_env), then run:
			//   python -c "import sys, json; print(json.dumps(sys.executable))"
			// Then set "command" to that path (e.g., "C:\\path\\to\\python.exe")
			"command": "python",
			"args": ["server.py"]
		}
	},
	"inputs": []
}